package com.fr.station.component.report.dao;

import java.util.List;

import com.fr.station.common.bean.report.ClassChangeBean;

public interface ClassChangeDAO {

	/**
	 * get each class records in order to display in the jsp page.
	 * 
	 * @param args which is retrieved in the input jsp page
	 * @param ClassChangeBean classChangeBean include the search criteria.
	 * 
	 * @return return list which is loaded in DB against the search criteria.
	 */
	public List<ClassChangeBean> getClassRecordsByCriteria(ClassChangeBean classChangeBean);

	/**
	 * get each card account records in order to display in the jsp page.
	 * 
	 * @param args which is retrieved in the input jsp page
	 * @param ClassChangeBean classChangeBean include the search criteria.
	 * 
	 * @return return list which is loaded in DB against the search criteria.
	 */
	public List<ClassChangeBean> getCardStorageRecordsByCriteria(ClassChangeBean classChangeBean);

	/**
	 * get each station trade records in order to display in the jsp page.
	 * 
	 * @param args which is retrieved in the input jsp page
	 * @param ClassChangeBean classChangeBean include the search criteria.
	 * 
	 * @return return list which is loaded in DB against the search criteria.
	 */
	public List<ClassChangeBean> getStationTradeRecordsByCriteria(ClassChangeBean classChangeBean);

	/**
	 * get each invoice records in order to display in the jsp page.
	 * 
	 * @param args which is retrieved in the input jsp page
	 * @param ClassChangeBean classChangeBean include the search criteria.
	 * 
	 * @return return list which is loaded in DB against the search criteria.
	 */
	public List<ClassChangeBean> getInvoiceRecordsByCriteria(ClassChangeBean classChangeBean);

	/**
	 * get class summary records in order to display in the jsp page.
	 * 
	 * @param args which is retrieved in the input jsp page
	 * @param ClassChangeBean classChangeBean include the search criteria.
	 * 
	 * @return return list which is loaded in DB against the search criteria.
	 */
	public ClassChangeBean getClassSummaryRecordsByCriteria(ClassChangeBean classChangeBean);
}
